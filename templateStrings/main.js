//given two strings, use the template literal format to concatenate the two strings with an 'are' in the middle.

function templateStrings(ob, feat){
  return `${obj} are ${feat}`;
}

console.log(templateStrings('Dogs', 'fun'))//should log 'Dogs are fun'
